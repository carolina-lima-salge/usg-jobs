# ── Scraper intermediate outputs ────────────────────────────────────────────
# The three individual scraper CSVs (uga_jobs.csv, gatech_jobs.csv,
# gastate_jobs.csv) are intentionally NOT listed here — they are each
# committed by their own dedicated workflow so the merge workflow can read them.
#
# NOTE: scrapers/onehcm_jobs.csv is also NOT listed — committed by update_onehcm.yml
#
# Only the merged/intermediate files that are regenerated fresh on every merge
# run and never need to be stored in git are ignored below.

scrapers/usg_all_jobs.csv
scrapers/usg_all_jobs.json
scrapers/jobs.json

# Python / runtime artefacts
__pycache__/
*.py[cod]
*.pyo
.env
